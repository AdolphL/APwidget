<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.adolph.widget">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name="com.adolph.test.base.BaseApplication">

        <activity android:name="com.adolph.test.divideLine.DivideLineTestActivity">

        </activity>

        <activity android:name="com.adolph.test.swipeback.SwipeBackActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.adolph.test.swipeback.SwipeBackScrollActivity" />

    </application>

</manifest>